# Expo's current npm 10 builder resolves optional peers differently from the
# npm 11 version used to generate package-lock.json. Keep clean installs on the
# committed dependency graph for reproducible mobile preview builds.
legacy-peer-deps=true

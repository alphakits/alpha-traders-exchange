*.mp4 filter=lfs diff=lfs merge=lfs -text
*.pack filter=lfs diff=lfs merge=lfs -text

# Mobile delivery branch only: web media is served remotely and excluded by
# .easignore. Keep its LFS pointers during the initial GitHub checkout.
/public/files/** -filter -diff -merge

# Native Android build snapshot only; do not merge into the website branch.
# Website videos are streamed from production and are not bundled in the app.
*.mp4 -filter -diff -merge -text
*.pack filter=lfs diff=lfs merge=lfs -text
public/files/course/pdfs/*.pdf binary
